/* body 스타일 */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 20px;
    background-color: #f9f9f9;
}

/* container 스타일 */
.container {
    width: 90%;
    max-width: 1200px;
    margin: 0 auto;
}

/* 헤딩 스타일 */
h1 {
    font-size: 24px;
    color: #333;
    margin-bottom: 20px;
}

/* 네비게이션 스타일 */
nav {
    margin-bottom: 20px;
}

nav a, nav span {
    text-decoration: none;
    color: #007BFF; /* 하이퍼링크 색상 */
    margin: 0 5px;
    font-size: 14px;
}

nav span:hover {
    text-decoration: underline;
}

/* 폼 스타일 */
form {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 20px;
}

form label {
    font-size: 14px;
    color: #555;
    font-weight: bold;
    display: flex;
    align-items: center;
    gap: 5px;
}

form input {
    padding: 5px;
    font-size: 14px;
    border: 1px solid #ccc;
    border-radius: 4px;
    width: 150px;
    height: 30px; /* 버튼과 동일한 높이 */
    box-sizing: border-box;
}

form input::placeholder {
    color: #bbb;
}

/* 버튼 스타일 */
form button {
    background-color: #f5f5f5; /* 회색 */
    color: #333;
    border: 1px solid #ccc;
    padding: 0 15px;
    font-size: 14px;
    border-radius: 4px;
    cursor: pointer;
    height: 30px; /* 입력 필드와 동일한 높이 */
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
}

form button:hover {
    background-color: #e0e0e0;
}

/* 테이블 스타일 */
table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 20px;
}

table th, table td {
    border: 1px solid #ddd;
    padding: 8px;
    text-align: center;
    font-size: 14px;
}

table th {
    background-color: #4CAF50; /* 초록색 헤더 */
    color: white;
    font-weight: bold;
}

table tr:nth-child(even) {
    background-color: #f2f2f2;
}

table tr:hover {
    background-color: #ddd;
}
